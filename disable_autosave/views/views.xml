<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="disable_autosave.list">
      <field name="name">disable_autosave list</field>
      <field name="model">disable_autosave.disable_autosave</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </list>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="disable_autosave.action_window">
      <field name="name">disable_autosave window</field>
      <field name="res_model">disable_autosave.disable_autosave</field>
      <field name="view_mode">list,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="disable_autosave.action_server">
      <field name="name">disable_autosave server</field>
      <field name="model_id" ref="model_disable_autosave_disable_autosave"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "list,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="disable_autosave" id="disable_autosave.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="disable_autosave.menu_1" parent="disable_autosave.menu_root"/>
    <menuitem name="Menu 2" id="disable_autosave.menu_2" parent="disable_autosave.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="disable_autosave.menu_1_list" parent="disable_autosave.menu_1"
              action="disable_autosave.action_window"/>
    <menuitem name="Server to list" id="disable_autosave" parent="disable_autosave.menu_2"
              action="disable_autosave.action_server"/>
-->
  </data>
</odoo>
