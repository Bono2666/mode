<odoo>
  <data>

    <!-- ================================================================= -->
    <!-- ACCOUNT TYPE VIEWS                                                 -->
    <!-- ================================================================= -->

    <record model="ir.ui.view" id="accounting_account_type_tree">
      <field name="name">Account Type</field>
      <field name="model">accounting.account.type</field>
      <field name="arch" type="xml">
        <tree>
          <field name="code"/>
          <field name="name"/>
          <field name="sequence"/>
        </tree>
      </field>
    </record>

    <record id="accounting_account_type_form" model="ir.ui.view">
      <field name="name">accounting.account.type.form</field>
      <field name="model">accounting.account.type</field>
      <field name="arch" type="xml">
      <form string="Account Type" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this data permanently?" invisible="not id or is_edit or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or user_can_delete"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <h1>
              <div class="d-flex">
                <field name="name" placeholder="e.g. Receivable" readonly="not is_edit and id"/>
              </div>
            </h1>
          </div>
          <group>
            <group>
              <field name="code" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="sequence" readonly="not is_edit and id"/>
            </group>
          </group>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="accounting_account_type_search">
      <field name="name">Account Type Search</field>
      <field name="model">accounting.account.type</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="code"/>
        </search>
      </field>
    </record>

    <!-- ================================================================= -->
    <!-- CHART OF ACCOUNTS VIEWS                                            -->
    <!-- ================================================================= -->

    <record model="ir.ui.view" id="accounting_account_tree">
      <field name="name">Chart of Accounts</field>
      <field name="model">accounting.account</field>
      <field name="arch" type="xml">
        <tree>
          <field name="code"/>
          <field name="name"/>
          <field name="type_id"/>
          <field name="currency_id"/>
          <field name="reconcile"/>
          <field name="active"/>
        </tree>
      </field>
    </record>

    <record id="accounting_account_form" model="ir.ui.view">
      <field name="name">accounting.account.form</field>
      <field name="model">accounting.account</field>
      <field name="arch" type="xml">
      <form string="Account" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this data permanently?" invisible="not id or is_edit or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or user_can_delete"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <field name="code"/>
            <h1>
              <div class="d-flex">
                <field name="name" placeholder="e.g. Accounts Receivable" readonly="not is_edit and id"/>
              </div>
            </h1>
          </div>
          <group>
            <group>
              <field name="type_id" readonly="not is_edit and id"/>
              <field name="parent_id" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="reconcile" readonly="not is_edit and id"/>
              <field name="active" readonly="not is_edit and id"/>
              <field name="currency_id" readonly="not is_edit and id"/>
            </group>
          </group>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="accounting_account_search">
      <field name="name">Account Search</field>
      <field name="model">accounting.account</field>
      <field name="arch" type="xml">
        <search>
          <field name="code"/>
          <field name="name"/>
          <field name="type_id"/>
          <filter string="Active" name="active" domain="[('active','=',True)]"/>
          <filter string="Reconcilable" name="reconcile" domain="[('reconcile','=',True)]"/>
        </search>
      </field>
    </record>

    <!-- ================================================================= -->
    <!-- JOURNAL VIEWS                                                     -->
    <!-- ================================================================= -->

    <record model="ir.ui.view" id="accounting_journal_tree">
      <field name="name">Journal</field>
      <field name="model">accounting.journal</field>
      <field name="arch" type="xml">
        <tree>
          <field name="code"/>
          <field name="name"/>
          <field name="type"/>
          <field name="default_debit_account_id"/>
          <field name="default_credit_account_id"/>
          <field name="active"/>
        </tree>
      </field>
    </record>

    <record id="accounting_journal_form" model="ir.ui.view">
      <field name="name">accounting.journal.form</field>
      <field name="model">accounting.journal</field>
      <field name="arch" type="xml">
      <form string="Journal" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this data permanently?" invisible="not id or is_edit or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or user_can_delete"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <h1>
              <div class="d-flex">
                <field name="name" placeholder="e.g. Sales Journal" readonly="not is_edit and id"/>
              </div>
            </h1>
          </div>
          <group>
            <group>
              <field name="code" readonly="not is_edit and id"/>
              <field name="type" readonly="not is_edit and id"/>
              <field name="active" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="default_debit_account_id" readonly="not is_edit and id"/>
              <field name="default_credit_account_id" readonly="not is_edit and id"/>
            </group>
          </group>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="accounting_journal_search">
      <field name="name">Journal Search</field>
      <field name="model">accounting.journal</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="code"/>
          <field name="type"/>
          <filter string="Active" name="active" domain="[('active','=',True)]"/>
        </search>
      </field>
    </record>

    <!-- ================================================================= -->
    <!-- FISCAL YEAR VIEWS                                                 -->
    <!-- ================================================================= -->

    <record model="ir.ui.view" id="accounting_fiscal_year_tree">
      <field name="name">Fiscal Year</field>
      <field name="model">accounting.fiscal.year</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="date_from"/>
          <field name="date_to"/>
          <field name="state" widget="badge" decoration-success="state == 'open'" decoration-danger="state == 'closed'"/>
        </tree>
      </field>
    </record>

    <record id="accounting_fiscal_year_form" model="ir.ui.view">
      <field name="name">accounting.fiscal.year.form</field>
      <field name="model">accounting.fiscal.year</field>
      <field name="arch" type="xml">
      <form string="Fiscal Year" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <field name="state" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or state == 'closed' or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or state == 'closed' or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this data permanently?" invisible="not id or is_edit or state == 'closed' or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or state == 'closed' or user_can_delete"/>
          <button string="Close Year" name="action_close_year" type="object" class="btn-warning" invisible="state != 'open'"/>
          <button string="Open Year" name="action_open_year" type="object" class="btn-primary" invisible="state != 'closed'"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <h1>
              <div class="d-flex">
                <field name="name" placeholder="e.g. Fiscal Year 2026" readonly="not is_edit and id"/>
              </div>
            </h1>
          </div>
          <group>
            <group>
              <field name="date_from" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="date_to" readonly="not is_edit and id"/>
            </group>
          </group>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="accounting_fiscal_year_search">
      <field name="name">Fiscal Year Search</field>
      <field name="model">accounting.fiscal.year</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="date_from"/>
          <filter string="Open" name="open" domain="[('state','=','open')]"/>
          <filter string="Closed" name="closed" domain="[('state','=','closed')]"/>
        </search>
      </field>
    </record>

    <!-- ================================================================= -->
    <!-- PERIOD VIEWS                                                      -->
    <!-- ================================================================= -->

    <record model="ir.ui.view" id="accounting_period_tree">
      <field name="name">Period</field>
      <field name="model">accounting.period</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="fiscal_year_id"/>
          <field name="date_from"/>
          <field name="date_to"/>
          <field name="state" widget="badge" decoration-success="state == 'open'" decoration-danger="state == 'closed'"/>
        </tree>
      </field>
    </record>

    <record id="accounting_period_form" model="ir.ui.view">
      <field name="name">accounting.period.form</field>
      <field name="model">accounting.period</field>
      <field name="arch" type="xml">
      <form string="Period" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <field name="state" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or state == 'closed' or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or state == 'closed' or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this data permanently?" invisible="not id or is_edit or state == 'closed' or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or state == 'closed' or user_can_delete"/>
          <button string="Close Period" name="action_close_period" type="object" class="btn-warning" invisible="state != 'open'"/>
          <button string="Open Period" name="action_open_period" type="object" class="btn-primary" invisible="state != 'closed'"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <h1>
              <div class="d-flex">
                <field name="name" placeholder="e.g. January 2026" readonly="not is_edit and id"/>
              </div>
            </h1>
          </div>
          <group>
            <group>
              <field name="fiscal_year_id" readonly="not is_edit and id"/>
              <field name="date_from" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="date_to" readonly="not is_edit and id"/>
            </group>
          </group>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="accounting_period_search">
      <field name="name">Period Search</field>
      <field name="model">accounting.period</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="fiscal_year_id"/>
          <field name="date_from"/>
          <filter string="Open" name="open" domain="[('state','=','open')]"/>
          <filter string="Closed" name="closed" domain="[('state','=','closed')]"/>
        </search>
      </field>
    </record>

    <!-- ================================================================= -->
    <!-- JOURNAL ENTRY (MOVE) VIEWS                                        -->
    <!-- ================================================================= -->

    <record model="ir.ui.view" id="accounting_move_tree">
      <field name="name">Journal Entry</field>
      <field name="model">accounting.move</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="date"/>
          <field name="journal_id"/>
          <field name="ref"/>
          <field name="total_debit" sum="Total Debit"/>
          <field name="total_credit" sum="Total Credit"/>
          <field name="state" widget="badge" decoration-success="state == 'posted'" decoration-warning="state == 'draft'" decoration-danger="state == 'cancel'"/>
        </tree>
      </field>
    </record>

    <record id="accounting_move_form" model="ir.ui.view">
      <field name="name">accounting.move.form</field>
      <field name="model">accounting.move</field>
      <field name="arch" type="xml">
      <form string="Journal Entry" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="state" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or state == 'posted' or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or state == 'posted' or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this journal entry?" invisible="not id or is_edit or state == 'posted' or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or state == 'posted' or user_can_delete"/>
          <button string="Post" name="action_post" type="object" class="btn-primary" invisible="state != 'draft' or is_edit"/>
          <button string="Cancel Entry" name="action_cancel" type="object" class="btn-warning" invisible="state != 'posted'"/>
          <button string="Reset to Draft" name="action_reset_to_draft" type="object" class="btn-secondary" invisible="state != 'cancel'"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <field name="name" readonly="1"/>
            <h1>
              <field name="ref" placeholder="e.g. January Rent" readonly="not is_edit and id"/>
            </h1>
          </div>
          <group>
            <group>
              <field name="journal_id" readonly="not is_edit and id"/>
              <field name="date" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="currency_id" readonly="not is_edit and id"/>
              <field name="state" readonly="1"/>
              <field name="posted_date" readonly="1"/>
            </group>
          </group>
          <notebook>
            <page string="Journal Items">
              <field name="line_ids" readonly="not is_edit and id">
                <tree editable="bottom">
                  <field name="sequence" widget="handle"/>
                  <field name="account_id" options="{'no_open': True, 'no_create': True}" required="1"/>
                  <field name="partner_id" options="{'no_open': True, 'no_create': True}"/>
                  <field name="name"/>
                  <field name="debit" widget="monetary" options="{'currency_field': 'currency_id'}" sum="Total Debit"/>
                  <field name="credit" widget="monetary" options="{'currency_field': 'currency_id'}" sum="Total Credit"/>
                  <field name="date_maturity"/>
                  <field name="currency_id" invisible="1"/>
                </tree>
              </field>
            </page>
          </notebook>
          <group class="oe_subtotal_footer">
            <group>
              <field name="total_debit" widget="monetary" options="{'currency_field': 'currency_id'}"/>
              <field name="total_credit" widget="monetary" options="{'currency_field': 'currency_id'}"/>
              <field name="is_balanced" widget="boolean_toggle" readonly="1"/>
            </group>
          </group>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="accounting_move_search">
      <field name="name">Journal Entry Search</field>
      <field name="model">accounting.move</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="ref"/>
          <field name="journal_id"/>
          <field name="date"/>
          <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
          <filter string="Posted" name="posted" domain="[('state','=','posted')]"/>
          <filter string="Cancelled" name="cancelled" domain="[('state','=','cancel')]"/>
          <separator/>
          <filter string="Today" name="today" date="date"/>
          <separator/>
          <filter string="This Month" name="this_month" date="date" default_period="this_month"/>
        </search>
      </field>
    </record>

    <!-- ================================================================= -->
    <!-- BANK STATEMENT VIEWS                                              -->
    <!-- ================================================================= -->

    <record model="ir.ui.view" id="accounting_bank_statement_tree">
      <field name="name">Bank Statement</field>
      <field name="model">accounting.bank.statement</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="date"/>
          <field name="journal_id"/>
          <field name="balance_start"/>
          <field name="balance_end"/>
          <field name="state" widget="badge" decoration-success="state == 'closed'" decoration-warning="state == 'open'"/>
        </tree>
      </field>
    </record>

    <record id="accounting_bank_statement_form" model="ir.ui.view">
      <field name="name">accounting.bank.statement.form</field>
      <field name="model">accounting.bank.statement</field>
      <field name="arch" type="xml">
      <form string="Bank Statement" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <field name="state" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or state == 'closed' or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or state == 'closed' or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this bank statement?" invisible="not id or is_edit or state == 'closed' or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or state == 'closed' or user_can_delete"/>
          <button string="Close" name="action_close_statement" type="object" class="btn-primary" invisible="state != 'open'"/>
          <button string="Reopen" name="action_open_statement" type="object" class="btn-secondary" invisible="state != 'closed'"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <h1>
              <field name="name" readonly="1"/>
            </h1>
          </div>
          <group>
            <group>
              <field name="journal_id" readonly="state != 'open'"/>
              <field name="date" readonly="state != 'open'"/>
            </group>
            <group>
              <field name="currency_id" readonly="state != 'open'"/>
              <field name="balance_start" widget="monetary" options="{'currency_field': 'currency_id'}" readonly="state != 'open'"/>
              <field name="balance_end" widget="monetary" options="{'currency_field': 'currency_id'}" readonly="1"/>
            </group>
          </group>
          <notebook>
            <page string="Statement Lines">
              <field name="line_ids" readonly="state == 'closed'">
                <tree editable="bottom">
                  <field name="date"/>
                  <field name="ref"/>
                  <field name="partner_id" options="{'no_create': True}"/>
                  <field name="amount" widget="monetary" options="{'currency_field': 'currency_id'}" sum="Total"/>
                  <field name="state" widget="badge"/>
                  <field name="currency_id" invisible="1"/>
                </tree>
              </field>
            </page>
          </notebook>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="accounting_bank_statement_search">
      <field name="name">Bank Statement Search</field>
      <field name="model">accounting.bank.statement</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="journal_id"/>
          <field name="date"/>
          <filter string="Open" name="open" domain="[('state','=','open')]"/>
          <filter string="Closed" name="closed" domain="[('state','=','closed')]"/>
        </search>
      </field>
    </record>

    <!-- ================================================================= -->
    <!-- REPORT WIZARD VIEWS                                               -->
    <!-- ================================================================= -->

    <record id="view_accounting_trial_balance_wizard_form" model="ir.ui.view">
      <field name="name">Trial Balance Wizard</field>
      <field name="model">accounting.trial.balance.wizard</field>
      <field name="arch" type="xml">
        <form string="Trial Balance">
          <group>
            <group>
              <field name="date_from"/>
              <field name="date_to"/>
            </group>
            <group>
              <field name="target_move"/>
            </group>
          </group>
          <footer>
            <button string="Generate" name="action_generate" type="object" class="btn-primary"/>
            <button string="Cancel" special="cancel" class="btn-secondary"/>
          </footer>
        </form>
      </field>
    </record>

    <record id="view_accounting_general_ledger_wizard_form" model="ir.ui.view">
      <field name="name">General Ledger Wizard</field>
      <field name="model">accounting.general.ledger.wizard</field>
      <field name="arch" type="xml">
        <form string="General Ledger">
          <group>
            <group>
              <field name="date_from"/>
              <field name="date_to"/>
            </group>
            <group>
              <field name="target_move"/>
              <field name="account_ids" widget="many2many_tags"/>
            </group>
          </group>
          <footer>
            <button string="Generate" name="action_generate" type="object" class="btn-primary"/>
            <button string="Cancel" special="cancel" class="btn-secondary"/>
          </footer>
        </form>
      </field>
    </record>

    <record id="view_accounting_aged_receivable_wizard_form" model="ir.ui.view">
      <field name="name">Aged Receivable Wizard</field>
      <field name="model">accounting.aged.receivable.wizard</field>
      <field name="arch" type="xml">
        <form string="Aged Receivable">
          <group>
            <group>
              <field name="date_as_of"/>
            </group>
            <group>
              <field name="period_length"/>
            </group>
          </group>
          <footer>
            <button string="Generate" name="action_generate" type="object" class="btn-primary"/>
            <button string="Cancel" special="cancel" class="btn-secondary"/>
          </footer>
        </form>
      </field>
    </record>

    <!-- Balance Sheet Wizard -->
    <record id="view_accounting_balance_sheet_wizard_form" model="ir.ui.view">
      <field name="name">Balance Sheet Wizard</field>
      <field name="model">accounting.balance.sheet.wizard</field>
      <field name="arch" type="xml">
        <form string="Balance Sheet">
          <group>
            <group>
              <field name="date_as_of"/>
            </group>
            <group>
              <field name="target_move"/>
            </group>
          </group>
          <footer>
            <button string="Generate" name="action_generate" type="object" class="btn-primary"/>
            <button string="Cancel" special="cancel" class="btn-secondary"/>
          </footer>
        </form>
      </field>
    </record>

    <!-- Profit And Loss Wizard -->
    <record id="view_accounting_profit_loss_wizard_form" model="ir.ui.view">
      <field name="name">Profit And Loss Wizard</field>
      <field name="model">accounting.profit.loss.wizard</field>
      <field name="arch" type="xml">
        <form string="Profit And Loss">
          <group>
            <group>
              <field name="date_from"/>
              <field name="date_to"/>
            </group>
            <group>
              <field name="target_move"/>
            </group>
          </group>
          <footer>
            <button string="Generate" name="action_generate" type="object" class="btn-primary"/>
            <button string="Cancel" special="cancel" class="btn-secondary"/>
          </footer>
        </form>
      </field>
    </record>

    <!-- Sales Order Profitability Wizard -->
    <record id="view_accounting_sales_profitability_wizard_form" model="ir.ui.view">
      <field name="name">Sales Order Profitability Wizard</field>
      <field name="model">accounting.sales_profitability_report.wizard</field>
      <field name="arch" type="xml">
        <form string="Sales Order Profitability">
          <group>
            <group>
              <field name="date_from"/>
              <field name="date_to"/>
            </group>
            <group>
              <field name="customer_id"/>
              <field name="sale_order_ids" widget="many2many_tags"/>
            </group>
          </group>
          <footer>
            <button string="Tampilkan" name="action_generate" type="object" class="btn-primary"/>
            <button string="Cetak PDF" name="action_print" type="object" class="btn-secondary"/>
            <button string="Cancel" special="cancel" class="btn-secondary"/>
          </footer>
        </form>
      </field>
    </record>

    <!-- ================================================================= -->
    <!-- REPORT VIEWS                                                      -->
    <!-- ================================================================= -->

    <record model="ir.ui.view" id="view_accounting_trial_balance_report_tree">
      <field name="name">Trial Balance Report</field>
      <field name="model">accounting.trial.balance.report</field>
      <field name="arch" type="xml">
        <tree create="false" delete="false" edit="false">
          <field name="account_code"/>
          <field name="account_name"/>
          <field name="type_name"/>
          <field name="total_debit" sum="Total Debit"/>
          <field name="total_credit" sum="Total Credit"/>
          <field name="balance" sum="Net Balance"/>
        </tree>
      </field>
    </record>

    <record model="ir.ui.view" id="view_accounting_general_ledger_report_tree">
      <field name="name">General Ledger Report</field>
      <field name="model">accounting.general.ledger.report</field>
      <field name="arch" type="xml">
        <tree create="false" delete="false" edit="false">
          <field name="move_date"/>
          <field name="move_name"/>
          <field name="journal_name"/>
          <field name="account_code"/>
          <field name="account_name"/>
          <field name="line_name"/>
          <field name="partner_name"/>
          <field name="debit" sum="Total Debit"/>
          <field name="credit" sum="Total Credit"/>
          <field name="ref"/>
        </tree>
      </field>
    </record>

    <record model="ir.ui.view" id="view_accounting_aged_receivable_report_tree">
      <field name="name">Aged Receivable Report</field>
      <field name="model">accounting.aged.receivable.report</field>
      <field name="arch" type="xml">
        <tree create="false" delete="false" edit="false">
          <field name="partner_name"/>
          <field name="total" sum="Total"/>
          <field name="age_0_30" sum="0–30 Days"/>
          <field name="age_31_60" sum="31–60 Days"/>
          <field name="age_61_90" sum="61–90 Days"/>
          <field name="age_90_plus" sum="90+ Days"/>
        </tree>
      </field>
    </record>

    <!-- Balance Sheet Report Tree -->
    <record model="ir.ui.view" id="view_accounting_balance_sheet_report_tree">
      <field name="name">Balance Sheet Report</field>
      <field name="model">accounting.balance.sheet.report</field>
      <field name="arch" type="xml">
        <tree create="false" delete="false" edit="false">
          <field name="category"/>
          <field name="account_code"/>
          <field name="account_name"/>
          <field name="type_name"/>
          <field name="balance" sum="Total"/>
        </tree>
      </field>
    </record>

    <!-- Profit And Loss Report Tree -->
    <record model="ir.ui.view" id="view_accounting_profit_loss_report_tree">
      <field name="name">Profit And Loss Report</field>
      <field name="model">accounting.profit.loss.report</field>
      <field name="arch" type="xml">
        <tree create="false" delete="false" edit="false">
          <field name="category"/>
          <field name="account_code"/>
          <field name="account_name"/>
          <field name="type_name"/>
          <field name="balance" sum="Net Total"/>
        </tree>
      </field>
    </record>

    <!-- Sales Order Profitability Report Tree -->
    <record model="ir.ui.view" id="view_accounting_sales_profitability_report_tree">
      <field name="name">Sales Order Profitability Report</field>
      <field name="model">accounting.sales_profitability_report</field>
      <field name="arch" type="xml">
        <tree create="false" delete="false" edit="false">
          <field name="sale_order_id"/>
          <field name="sale_order_date"/>
          <field name="customer_id"/>
          <field name="has_transaction" widget="badge" decoration-success="has_transaction == 'yes'" decoration-muted="has_transaction == 'no'"/>
          <field name="total_revenue" widget="monetary" options="{'currency_field': 'currency_id'}" sum="Total Revenue"/>
          <field name="cost_cogs" widget="monetary" options="{'currency_field': 'currency_id'}" sum="Total COGS"/>
          <field name="cost_commission" widget="monetary" options="{'currency_field': 'currency_id'}" sum="Total Commission"/>
          <field name="cost_supporting" widget="monetary" options="{'currency_field': 'currency_id'}" sum="Total Supporting"/>
          <field name="total_cost" widget="monetary" options="{'currency_field': 'currency_id'}" sum="Total Cost"/>
          <field name="margin_amount" widget="monetary" options="{'currency_field': 'currency_id'}" sum="Total Margin" decoration-danger="margin_amount &lt; 0"/>
          <field name="margin_percent" decoration-danger="margin_percent &lt; 0"/>
          <field name="currency_id" invisible="1"/>
        </tree>
      </field>
    </record>

    <!-- ================================================================= -->
    <!-- ACTIONS                                                           -->
    <!-- ================================================================= -->

    <record model="ir.actions.act_window" id="action_account_types">
      <field name="name">Account Types</field>
      <field name="res_model">accounting.account.type</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <record model="ir.actions.act_window" id="action_chart_of_accounts">
      <field name="name">Chart of Accounts</field>
      <field name="res_model">accounting.account</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <record model="ir.actions.act_window" id="action_accounting_journals">
      <field name="name">Journals</field>
      <field name="res_model">accounting.journal</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <record model="ir.actions.act_window" id="action_fiscal_years">
      <field name="name">Fiscal Years</field>
      <field name="res_model">accounting.fiscal.year</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <record model="ir.actions.act_window" id="action_periods">
      <field name="name">Periods</field>
      <field name="res_model">accounting.period</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <record model="ir.actions.act_window" id="action_accounting_moves">
      <field name="name">Journal Entries</field>
      <field name="res_model">accounting.move</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <record model="ir.actions.act_window" id="action_bank_statements">
      <field name="name">Bank Statements</field>
      <field name="res_model">accounting.bank.statement</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <!-- Report actions -->
    <record model="ir.actions.act_window" id="action_trial_balance_wizard">
      <field name="name">Trial Balance</field>
      <field name="res_model">accounting.trial.balance.wizard</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
    </record>

    <record model="ir.actions.act_window" id="action_general_ledger_wizard">
      <field name="name">General Ledger</field>
      <field name="res_model">accounting.general.ledger.wizard</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
    </record>

    <record model="ir.actions.act_window" id="action_aged_receivable_wizard">
      <field name="name">Aged Receivable</field>
      <field name="res_model">accounting.aged.receivable.wizard</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
    </record>

    <record model="ir.actions.act_window" id="action_balance_sheet_wizard">
      <field name="name">Balance Sheet</field>
      <field name="res_model">accounting.balance.sheet.wizard</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
    </record>

    <record model="ir.actions.act_window" id="action_profit_loss_wizard">
      <field name="name">Profit And Loss</field>
      <field name="res_model">accounting.profit.loss.wizard</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
    </record>

    <record model="ir.actions.act_window" id="action_sales_profitability_wizard">
      <field name="name">Sales Order Profitability</field>
      <field name="res_model">accounting.sales_profitability_report.wizard</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
    </record>

    <!-- ================================================================= -->
    <!-- MENU ITEMS                                                        -->
    <!-- ================================================================= -->

    <!-- Top-level Accounting menu -->
    <menuitem name="Accounting" id="accounting_root_menu" parent="general.menu_root" sequence="4"/>

    <!-- Transactions submenu -->
    <menuitem name="Transactions" id="menu_accounting_transactions_parent" parent="accounting_root_menu" sequence="1"/>
    <menuitem name="Journal Entries" id="menu_journal_entries_act" parent="menu_accounting_transactions_parent" action="action_accounting_moves" sequence="1"/>

    <!-- Banking submenu -->
    <menuitem name="Banking" id="menu_accounting_banking_parent" parent="accounting_root_menu" sequence="2"/>
    <menuitem name="Bank Statements" id="menu_bank_statements_act" parent="menu_accounting_banking_parent" action="action_bank_statements" sequence="1"/>

    <!-- Ledger submenu -->
    <menuitem name="Ledger" id="menu_accounting_ledger_parent" parent="accounting_root_menu" sequence="3"/>
    <menuitem name="Trial Balance" id="menu_trial_balance_act" parent="menu_accounting_ledger_parent" action="action_trial_balance_wizard" sequence="1"/>
    <menuitem name="General Ledger" id="menu_general_ledger_act" parent="menu_accounting_ledger_parent" action="action_general_ledger_wizard" sequence="2"/>
    <menuitem name="Aged Receivable" id="menu_aged_receivable_act" parent="menu_accounting_ledger_parent" action="action_aged_receivable_wizard" sequence="3"/>

    <!-- Reporting submenu -->
    <menuitem name="Reporting" id="menu_accounting_reports_parent" parent="accounting_root_menu" sequence="4"/>
    <menuitem name="Balance Sheet" id="menu_balance_sheet_act" parent="menu_accounting_reports_parent" action="action_balance_sheet_wizard" sequence="1"/>
    <menuitem name="Profit And Loss" id="menu_profit_loss_act" parent="menu_accounting_reports_parent" action="action_profit_loss_wizard" sequence="2"/>
    <menuitem name="Sales Order Profitability" id="menu_sales_profitability_report_act" parent="menu_accounting_reports_parent" action="action_sales_profitability_wizard" sequence="3"/>

    <!-- RBAC: general.menu entry for Sales Order Profitability.
         The ir_ui_menu_id MUST point to the exact ir.ui.menu record above
         (record ref, not name match), so users without a general.auth entry
         for this menu are restricted from seeing it. Placed here (after the
         <menuitem>) so the ref resolves regardless of manifest load order. -->
    <record id="menu_sales_profitability_report" model="general.menu">
      <field name="menu_id">sales_profitability_report</field>
      <field name="menu_name">Sales Order Profitability</field>
      <field name="parent_menu">accounting_reports</field>
      <field name="ir_ui_menu_id" ref="accounting.menu_sales_profitability_report_act"/>
    </record>

    <!-- Accounting config under main Configuration -->
    <menuitem name="Accounting Configuration" id="menu_accounting_config_parent" parent="general.menu_master_menu" sequence="4"/>
    <menuitem name="Chart of Accounts" id="menu_chart_of_accounts_act" parent="menu_accounting_config_parent" action="action_chart_of_accounts" sequence="1"/>
    <menuitem name="Account Types" id="menu_account_types_act" parent="menu_accounting_config_parent" action="action_account_types" sequence="2"/>
    <menuitem name="Journals" id="menu_journals_act" parent="menu_accounting_config_parent" action="action_accounting_journals" sequence="3"/>
    <menuitem name="Fiscal Years" id="menu_fiscal_years_act" parent="menu_accounting_config_parent" action="action_fiscal_years" sequence="4"/>
    <menuitem name="Periods" id="menu_periods_act" parent="menu_accounting_config_parent" action="action_periods" sequence="5"/>

    <!-- ================================================================= -->
    <!-- VIEW INHERITANCE: Smart Buttons on existing forms                  -->
    <!-- ================================================================= -->

    <!-- Add Accounting Entry button to Sales Invoice form -->
    <record id="view_sales_invoice_accounting_inherit" model="ir.ui.view">
      <field name="name">sales.invoice.accounting.inherit</field>
      <field name="model">sales.invoice</field>
      <field name="inherit_id" ref="sales.sales_invoice_form_view"/>
      <field name="arch" type="xml">
        <xpath expr="//header" position="inside">
          <field name="accounting_move_id" invisible="1"/>
          <field name="accounting_move_count" invisible="1"/>
          <button string="Journal Entry" name="action_view_accounting_move" type="object"
                  class="btn-secondary" invisible="not accounting_move_id"/>
        </xpath>
      </field>
    </record>

    <!-- Add Accounting Entry button to Sales Payment form -->
    <record id="view_sales_payment_accounting_inherit" model="ir.ui.view">
      <field name="name">sales.payment.accounting.inherit</field>
      <field name="model">sales.payment</field>
      <field name="inherit_id" ref="sales.sales_payment_form_view"/>
      <field name="arch" type="xml">
        <xpath expr="//header" position="inside">
          <field name="accounting_move_id" invisible="1"/>
          <button string="Journal Entry" name="action_view_accounting_move" type="object"
                  class="btn-secondary" invisible="not accounting_move_id"/>
        </xpath>
      </field>
    </record>

    <!-- Add Accounting Entry button to Purchase Bill form -->
    <record id="view_purchases_bill_accounting_inherit" model="ir.ui.view">
      <field name="name">purchases.bill.accounting.inherit</field>
      <field name="model">purchases.bill</field>
      <field name="inherit_id" ref="purchases.purchases_bill_form"/>
      <field name="arch" type="xml">
        <xpath expr="//header" position="inside">
          <field name="accounting_move_id" invisible="1"/>
          <field name="accounting_move_count" invisible="1"/>
          <button string="Journal Entry" name="action_view_accounting_move" type="object"
                  class="btn-secondary" invisible="not accounting_move_id"/>
        </xpath>
      </field>
    </record>

    <!-- Add Account Properties tab to Product Category form -->
    <record id="view_product_category_account_inherit" model="ir.ui.view">
      <field name="name">product.category.account.inherit</field>
      <field name="model">sales.product_category</field>
      <field name="inherit_id" ref="sales.product_category_view_form"/>
      <field name="arch" type="xml">
        <xpath expr="//div[@class='oe_title']" position="after">
          <notebook>
            <page string="Account Properties" name="account_properties">
              <field name="id" invisible="1"/>
              <field name="is_edit" invisible="1"/>
              <group>
                <group>
                  <field name="income_account_id" readonly="not is_edit and id" options="{'no_open': True, 'no_create': True}"/>
                  <field name="expense_account_id" readonly="not is_edit and id" options="{'no_open': True, 'no_create': True}"/>
                  <field name="stock_account_id" readonly="not is_edit and id" options="{'no_open': True, 'no_create': True}"/>
                </group>
              </group>
            </page>
          </notebook>
        </xpath>
      </field>
    </record>

    <!-- Add Commission button to Sales Invoice form -->
    <record id="view_sales_invoice_commission_inherit" model="ir.ui.view">
      <field name="name">sales.invoice.commission.inherit</field>
      <field name="model">sales.invoice</field>
      <field name="inherit_id" ref="sales.sales_invoice_form_view"/>
      <field name="arch" type="xml">
        <xpath expr="//header" position="inside">
          <field name="commission_amount" invisible="1"/>
          <field name="commission_count" invisible="1"/>
          <button string="Commissions" name="action_view_commission_settlements" type="object"
                  class="btn-secondary" invisible="commission_count == 0"/>
        </xpath>
      </field>
    </record>

    <!-- ================================================================= -->
    <!-- COMMISSION PLANS VIEWS                                             -->
    <!-- ================================================================= -->

    <!-- Commission Plan Tree -->
    <record model="ir.ui.view" id="view_commission_plan_tree">
      <field name="name">Commission Plan Tree</field>
      <field name="model">accounting.commission.plan</field>
      <field name="arch" type="xml">
        <tree>
          <field name="code"/>
          <field name="name"/>
          <field name="type"/>
          <field name="rate"/>
          <field name="based_on"/>
          <field name="active"/>
        </tree>
      </field>
    </record>

    <!-- Commission Plan Form -->
    <record model="ir.ui.view" id="view_commission_plan_form">
      <field name="name">Commission Plan Form</field>
      <field name="model">accounting.commission.plan</field>
      <field name="arch" type="xml">
        <form create="false">
          <header>
            <field name="id" invisible="1"/>
            <field name="is_edit" invisible="1"/>
            <field name="user_can_update" invisible="1"/>
            <field name="user_can_delete" invisible="1"/>
            <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or user_can_update"/>
            <button string="Save" name="action_save" type="object" invisible="id and not is_edit" class="oe_highlight"/>
            <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
            <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
            <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this data permanently?" invisible="not id or is_edit or not user_can_delete"/>
            <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or user_can_delete"/>
            <span class="min-w-0 text-truncate mt-1">
              <field name="model_description"/>
            </span>
            <field name="active" widget="boolean_toggle" invisible="not is_edit"/>
            <style>
              .o_control_panel {
                display: none !important;
              }
            </style>
          </header>
          <sheet>
            <div class="oe_title">
              <field name="code"/>
              <h1>
                <div class="d-flex">
                  <field class="text-break" name="name" options="{'line_breaks': False}" widget="text" placeholder="e.g. Standard Commission 5%" readonly="not is_edit and id"/>
                </div>
              </h1>
            </div>
            <group>
              <group>
                <field name="type" readonly="not is_edit and id"/>
                <field name="rate" readonly="not is_edit and id"/>
                <field name="based_on" readonly="not is_edit and id"/>
              </group>
              <group>
                <field name="journal_id" readonly="not is_edit and id"/>
                <field name="expense_account_id" readonly="not is_edit and id"/>
                <field name="payable_account_id" readonly="not is_edit and id"/>
              </group>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- Commission Settlement Tree -->
    <record model="ir.ui.view" id="view_commission_settlement_tree">
      <field name="name">Commission Settlement Tree</field>
      <field name="model">accounting.commission.settlement</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="date"/>
          <field name="salesperson_id"/>
          <field name="plan_id"/>
          <field name="base_amount"/>
          <field name="rate"/>
          <field name="commission_amount"/>
          <field name="state"/>
        </tree>
      </field>
    </record>

    <!-- Commission Settlement Form -->
    <record model="ir.ui.view" id="view_commission_settlement_form">
      <field name="name">Commission Settlement Form</field>
      <field name="model">accounting.commission.settlement</field>
      <field name="arch" type="xml">
        <form>
          <header>
            <field name="is_edit" invisible="1"/>
            <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
            <button string="Post" name="action_post" type="object" class="btn-primary" invisible="state != 'draft'"/>
            <button string="Cancel Settlement" name="action_cancel" type="object" class="btn-secondary" invisible="state not in ['draft', 'posted']"/>
            <button string="Journal Entry" name="action_view_move" type="object" class="btn-secondary" invisible="not move_id"/>
            <span class="min-w-0 text-truncate mt-1">
              <field name="model_description"/>
            </span>
            <field name="state" widget="statusbar" statusbar_visible="draft,posted"/>
            <style>
              .o_control_panel {
                display: none !important;
              }
            </style>
          </header>
          <sheet>
            <div class="oe_title">
              <field name="name"/>
              <h1>
                <div class="d-flex">
                  <field class="text-break" name="salesperson_id" options="{'line_breaks': False}" widget="text" readonly="1"/>
                </div>
              </h1>
            </div>
            <group>
              <group>
                <field name="date" readonly="1"/>
                <field name="plan_id" readonly="1"/>
                <field name="invoice_id" readonly="1"/>
              </group>
              <group>
                <field name="base_amount" readonly="1"/>
                <field name="rate" readonly="1"/>
                <field name="commission_amount" readonly="1"/>
                <field name="move_id" invisible="1"/>
              </group>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- Commission Plan Action -->
    <record model="ir.actions.act_window" id="action_commission_plans">
      <field name="name">Commission Plans</field>
      <field name="res_model">accounting.commission.plan</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <!-- Commission Settlement Action -->
    <record model="ir.actions.act_window" id="action_commission_settlements">
      <field name="name">Commission Settlements</field>
      <field name="res_model">accounting.commission.settlement</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <!-- ================================================================= -->
    <!-- COMMISSION MENU ITEMS                                              -->
    <!-- ================================================================= -->

    <menuitem name="Commissions" id="menu_accounting_commissions_parent" parent="accounting_root_menu" sequence="5"/>
    <menuitem name="Commission Plans" id="menu_commission_plans_act" parent="menu_accounting_commissions_parent" action="action_commission_plans" sequence="1"/>
    <menuitem name="Commission Settlements" id="menu_commission_settlements_act" parent="menu_accounting_commissions_parent" action="action_commission_settlements" sequence="2"/>

    <!-- ================================================================= -->
    <!-- REPORT ACTIONS (PDF)                                               -->
    <!-- ================================================================= -->

    <record id="action_report_balance_sheet" model="ir.actions.report">
      <field name="name">Balance Sheet</field>
      <field name="model">accounting.balance.sheet.report</field>
      <field name="report_type">qweb-html</field>
      <field name="report_name">accounting.report_balance_sheet_template</field>
      <field name="report_file">accounting.report_balance_sheet_template</field>
      <field name="print_report_name">'Balance Sheet - %s' % (context.get('date_as_of', '') or '')</field>
    </record>

    <record id="action_report_profit_loss" model="ir.actions.report">
      <field name="name">Profit And Loss</field>
      <field name="model">accounting.profit.loss.report</field>
      <field name="report_type">qweb-html</field>
      <field name="report_name">accounting.report_profit_loss_template</field>
      <field name="report_file">accounting.report_profit_loss_template</field>
      <field name="print_report_name">'P&amp;L - %s s/d %s' % (context.get('date_from', '') or '', context.get('date_to', '') or '')</field>
    </record>

    <record id="action_report_sales_profitability" model="ir.actions.report">
      <field name="name">Sales Order Profitability</field>
      <field name="model">accounting.sales_profitability_report</field>
      <field name="report_type">qweb-html</field>
      <field name="report_name">accounting.report_sales_profitability_template</field>
      <field name="report_file">accounting.report_sales_profitability_template</field>
      <field name="print_report_name">'Sales Order Profitability - %s s/d %s' % (context.get('date_from', '') or '', context.get('date_to', '') or '')</field>
    </record>

    <!-- ================================================================= -->
    <!-- PETTY CASH VIEWS                                                  -->
    <!-- ================================================================= -->

    <!-- Petty Cash Fund Tree -->
    <record model="ir.ui.view" id="view_petty_cash_tree">
      <field name="name">Petty Cash Fund</field>
      <field name="model">accounting.petty.cash</field>
      <field name="arch" type="xml">
        <tree>
          <field name="code"/>
          <field name="name"/>
          <field name="responsible_employee_id"/>
          <field name="journal_id"/>
          <field name="default_cash_account_id"/>
          <field name="current_balance" widget="monetary" options="{'currency_field': 'currency_id'}" sum="Total"/>
          <field name="active"/>
        </tree>
      </field>
    </record>

    <!-- Petty Cash Fund Form -->
    <record id="view_petty_cash_form" model="ir.ui.view">
      <field name="name">accounting.petty.cash.form</field>
      <field name="model">accounting.petty.cash</field>
      <field name="arch" type="xml">
      <form string="Petty Cash Fund" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this fund permanently?" invisible="not id or is_edit or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or user_can_delete"/>
          <button string="Refresh Balance" name="action_refresh_balance" type="object" class="btn-secondary" invisible="not id"/>
          <button string="Expenses" name="action_view_expenses" type="object" class="btn-secondary" invisible="not id"/>
          <button string="Top Ups" name="action_view_topups" type="object" class="btn-secondary" invisible="not id"/>
          <button string="Transfers" name="action_view_transfers" type="object" class="btn-secondary" invisible="not id"/>
          <button string="Settlements" name="action_view_settlements" type="object" class="btn-secondary" invisible="not id"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <h1>
              <div class="d-flex">
                <field name="name" placeholder="e.g. Main Office Cash" readonly="not is_edit and id"/>
              </div>
            </h1>
          </div>
          <group>
            <group>
              <field name="code" readonly="not is_edit and id"/>
              <field name="responsible_employee_id" readonly="not is_edit and id"/>
              <field name="active" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="journal_id" readonly="not is_edit and id"/>
              <field name="currency_id" readonly="not is_edit and id"/>
              <field name="current_balance" widget="monetary" options="{'currency_field': 'currency_id'}"/>
            </group>
          </group>
          <group string="Default Accounts">
            <group>
              <field name="default_cash_account_id" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="default_expense_account_id" readonly="not is_edit and id"/>
            </group>
          </group>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="view_petty_cash_search">
      <field name="name">Petty Cash Fund Search</field>
      <field name="model">accounting.petty.cash</field>
      <field name="arch" type="xml">
        <search>
          <field name="code"/>
          <field name="name"/>
          <field name="responsible_employee_id"/>
          <filter string="Active" name="active" domain="[('active','=',True)]"/>
        </search>
      </field>
    </record>

    <!-- Expense Category Tree -->
    <record model="ir.ui.view" id="view_pc_category_tree">
      <field name="name">Expense Category</field>
      <field name="model">accounting.petty.cash.category</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="expense_account_id"/>
          <field name="tax_id"/>
          <field name="active"/>
        </tree>
      </field>
    </record>

    <!-- Expense Category Form -->
    <record id="view_pc_category_form" model="ir.ui.view">
      <field name="name">accounting.petty.cash.category.form</field>
      <field name="model">accounting.petty.cash.category</field>
      <field name="arch" type="xml">
      <form string="Expense Category" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this category permanently?" invisible="not id or is_edit or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or user_can_delete"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <h1>
              <div class="d-flex">
                <field name="name" placeholder="e.g. Transportation" readonly="not is_edit and id"/>
              </div>
            </h1>
          </div>
          <group>
            <group>
              <field name="expense_account_id" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="tax_id" readonly="not is_edit and id"/>
              <field name="active" readonly="not is_edit and id"/>
            </group>
          </group>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="view_pc_category_search">
      <field name="name">Expense Category Search</field>
      <field name="model">accounting.petty.cash.category</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="expense_account_id"/>
          <filter string="Active" name="active" domain="[('active','=',True)]"/>
        </search>
      </field>
    </record>

    <!-- Cash Expense Tree -->
    <record model="ir.ui.view" id="view_pc_expense_tree">
      <field name="name">Cash Expense</field>
      <field name="model">accounting.petty.cash.expense</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="date"/>
          <field name="fund_id"/>
          <field name="type"/>
          <field name="employee_id"/>
          <field name="sales_order_id"/>
          <field name="total_amount" widget="monetary" sum="Total"/>
          <field name="state" widget="badge" decoration-success="state == 'posted'" decoration-warning="state in ('draft','wait_approval','approved')" decoration-danger="state == 'cancelled'"/>
        </tree>
      </field>
    </record>

    <!-- Cash Expense Form -->
    <record id="view_pc_expense_form" model="ir.ui.view">
      <field name="name">accounting.petty.cash.expense.form</field>
      <field name="model">accounting.petty.cash.expense</field>
      <field name="arch" type="xml">
      <form string="Cash Expense" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <field name="state" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or state == 'posted' or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or state == 'posted' or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit or context.get('is_approval_view')" class="oe_highlight"/>
          <button string="Save" name="action_save_revised" type="object" invisible="id and not is_edit or not context.get('is_approval_view')" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this expense permanently?" invisible="not id or is_edit or state == 'posted' or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or state == 'posted' or user_can_delete"/>
          <button string="Post" name="action_post" type="object" class="btn-primary" invisible="not id or ((state != 'draft' or need_approval or is_edit) and (state != 'approved' or not need_approval))"/>
          <button string="Post" class="opacity-50 pe-none" invisible="not id or not ((state != 'draft' or need_approval or is_edit) and (state != 'approved' or not need_approval))"/>
          <button string="Submit" name="action_submit_for_approval" type="object" class="btn-primary" invisible="state != 'draft' or not need_approval or is_edit or not user_can_submit"/>
          <button string="Approve" name="action_approve" type="object" class="btn-primary" invisible="not id or is_edit or not user_can_approve or not context.get('is_approval_view')"/>
          <button string="Revise" name="action_revise" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_revise or not context.get('is_approval_view')"/>
          <button string="Return" name="action_return" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_return or not context.get('is_approval_view')"/>
          <button string="Reject" name="action_reject" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_reject or not context.get('is_approval_view')"/>
          <button string="Cancel Expense" name="action_cancel" type="object" class="btn-warning" invisible="not id or state != 'posted' or is_edit"/>
          <button string="Reset to Draft" name="action_reset_to_draft" type="object" class="btn-secondary" invisible="state != 'cancelled'"/>
          <button string="Journal Entry" name="action_view_move" type="object" class="btn-secondary" invisible="not move_id"/>
          <field name="state" widget="statusbar" statusbar_visible="draft,wait_approval,approved,posted"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <field name="need_approval" invisible="1"/>
          <field name="approval_status" invisible="1"/>
          <field name="current_approver" invisible="1"/>
          <field name="current_approver_name" invisible="1"/>
          <field name="user_can_approve" invisible="1"/>
          <field name="user_can_revise" invisible="1"/>
          <field name="user_can_return" invisible="1"/>
          <field name="user_can_reject" invisible="1"/>
          <field name="user_can_submit" invisible="1"/>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <field name="name" readonly="1"/>
            <h1>
              <field name="description" placeholder="e.g. Pembelian ATK Januari" readonly="not is_edit and id"/>
            </h1>
          </div>
          <group>
            <group>
              <field name="date" readonly="not is_edit and id"/>
              <field name="fund_id" readonly="not is_edit and id"/>
              <field name="type" readonly="not is_edit and id"/>
              <field name="sales_order_id" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="employee_id" readonly="not is_edit and id"/>
              <field name="total_amount" widget="monetary" readonly="1"/>
              <field name="move_id" invisible="1"/>
            </group>
          </group>
          <notebook>
            <page string="Expense Lines">
              <field name="line_ids" readonly="not is_edit and id">
                <tree editable="bottom">
                  <field name="sequence" widget="handle"/>
                  <field name="category_id" options="{'no_open': True, 'no_create': True}" required="1"/>
                  <field name="description"/>
                  <field name="amount" widget="monetary" sum="Total"/>
                </tree>
              </field>
            </page>
            <page string="Approval Logs" name="approval_logs" invisible="not approval_log_ids">
              <field name="approval_log_ids" readonly="1">
                <tree default_order="sequence asc, id asc">
                  <field name="sequence" widget="handle"/>
                  <field name="approver" string="Approver Name"/>
                  <field name="position" string="Position/Role"/>
                  <field name="action_date" string="Date"/>
                  <field name="state" decoration-danger="state == 'rejected'" decoration-info="state == 'revised'" decoration-primary="state == 'approved'" decoration-warning="state == 'returned'" widget="badge" string="Status"/>
                  <field name="note" string="Notes/Feedback"/>
                  <field name="approval_reason" string="Approval Reason"/>
                </tree>
                <form string="Approval Log Detail">
                  <group>
                    <field name="approver"/>
                    <field name="position"/>
                    <field name="action_date"/>
                    <field name="state" widget="badge"/>
                  </group>
                  <separator string="Approval Reason"/>
                  <field name="approval_reason" readonly="1"/>
                  <separator string="Notes"/>
                  <field name="note" readonly="1"/>
                </form>
              </field>
            </page>
          </notebook>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="view_pc_expense_search">
      <field name="name">Cash Expense Search</field>
      <field name="model">accounting.petty.cash.expense</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="date"/>
          <field name="fund_id"/>
          <field name="employee_id"/>
          <field name="description"/>
          <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
          <filter string="Waiting Approval" name="wait_approval" domain="[('state','=','wait_approval')]"/>
          <filter string="To Approve" name="filter_to_approve" domain="[('current_approver','=',uid),('state','=','wait_approval')]"/>
          <filter string="Approved" name="approved" domain="[('state','=','approved')]"/>
          <filter string="Posted" name="posted" domain="[('state','=','posted')]"/>
          <filter string="Cancelled" name="cancelled" domain="[('state','=','cancelled')]"/>
          <separator/>
          <filter string="Today" name="today" date="date"/>
          <separator/>
          <filter string="This Month" name="this_month" date="date" default_period="this_month"/>
        </search>
      </field>
    </record>

    <!-- Top Up Tree -->
    <record model="ir.ui.view" id="view_pc_topup_tree">
      <field name="name">Top Up</field>
      <field name="model">accounting.petty.cash.topup</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="date"/>
          <field name="fund_id"/>
          <field name="source_account_id"/>
          <field name="amount" widget="monetary" sum="Total"/>
          <field name="state" widget="badge" decoration-success="state == 'posted'" decoration-warning="state in ('draft','wait_approval','approved')" decoration-danger="state == 'cancelled'"/>
        </tree>
      </field>
    </record>

    <!-- Top Up Form -->
    <record id="view_pc_topup_form" model="ir.ui.view">
      <field name="name">accounting.petty.cash.topup.form</field>
      <field name="model">accounting.petty.cash.topup</field>
      <field name="arch" type="xml">
      <form string="Top Up" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <field name="state" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or state == 'posted' or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or state == 'posted' or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit or context.get('is_approval_view')" class="oe_highlight"/>
          <button string="Save" name="action_save_revised" type="object" invisible="id and not is_edit or not context.get('is_approval_view')" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this top up permanently?" invisible="not id or is_edit or state == 'posted' or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or state == 'posted' or user_can_delete"/>
          <button string="Post" name="action_post" type="object" class="btn-primary" invisible="not id or ((state != 'draft' or need_approval or is_edit) and (state != 'approved' or not need_approval))"/>
          <button string="Post" class="opacity-50 pe-none" invisible="not id or not ((state != 'draft' or need_approval or is_edit) and (state != 'approved' or not need_approval))"/>
          <button string="Submit" name="action_submit_for_approval" type="object" class="btn-primary" invisible="state != 'draft' or not need_approval or is_edit or not user_can_submit"/>
          <button string="Approve" name="action_approve" type="object" class="btn-primary" invisible="not id or is_edit or not user_can_approve or not context.get('is_approval_view')"/>
          <button string="Revise" name="action_revise" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_revise or not context.get('is_approval_view')"/>
          <button string="Return" name="action_return" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_return or not context.get('is_approval_view')"/>
          <button string="Reject" name="action_reject" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_reject or not context.get('is_approval_view')"/>
          <button string="Cancel Top Up" name="action_cancel" type="object" class="btn-warning" invisible="not id or state != 'posted' or is_edit"/>
          <button string="Journal Entry" name="action_view_move" type="object" class="btn-secondary" invisible="not move_id"/>
          <field name="state" widget="statusbar" statusbar_visible="draft,wait_approval,approved,posted"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <field name="need_approval" invisible="1"/>
          <field name="approval_status" invisible="1"/>
          <field name="current_approver" invisible="1"/>
          <field name="current_approver_name" invisible="1"/>
          <field name="user_can_approve" invisible="1"/>
          <field name="user_can_revise" invisible="1"/>
          <field name="user_can_return" invisible="1"/>
          <field name="user_can_reject" invisible="1"/>
          <field name="user_can_submit" invisible="1"/>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <field name="name" readonly="1"/>
            <h1>
              <field name="reference" placeholder="e.g. Transfer dari BCA" readonly="not is_edit and id"/>
            </h1>
          </div>
          <group>
            <group>
              <field name="date" readonly="not is_edit and id"/>
              <field name="fund_id" readonly="not is_edit and id"/>
              <field name="source_account_id" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="amount" widget="monetary" readonly="not is_edit and id"/>
              <field name="move_id" invisible="1"/>
            </group>
          </group>
          <notebook>
            <page string="Approval Logs" name="approval_logs" invisible="not approval_log_ids">
              <field name="approval_log_ids" readonly="1">
                <tree default_order="sequence asc, id asc">
                  <field name="sequence" widget="handle"/>
                  <field name="approver" string="Approver Name"/>
                  <field name="position" string="Position/Role"/>
                  <field name="action_date" string="Date"/>
                  <field name="state" decoration-danger="state == 'rejected'" decoration-info="state == 'revised'" decoration-primary="state == 'approved'" decoration-warning="state == 'returned'" widget="badge" string="Status"/>
                  <field name="note" string="Notes/Feedback"/>
                </tree>
                <form string="Approval Log Detail">
                  <group>
                    <field name="approver"/>
                    <field name="position"/>
                    <field name="action_date"/>
                    <field name="state" widget="badge"/>
                  </group>
                  <separator string="Notes"/>
                  <field name="note" readonly="1"/>
                </form>
              </field>
            </page>
          </notebook>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="view_pc_topup_search">
      <field name="name">Top Up Search</field>
      <field name="model">accounting.petty.cash.topup</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="date"/>
          <field name="fund_id"/>
          <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
          <filter string="Waiting Approval" name="wait_approval" domain="[('state','=','wait_approval')]"/>
          <filter string="To Approve" name="filter_to_approve" domain="[('current_approver','=',uid),('state','=','wait_approval')]"/>
          <filter string="Approved" name="approved" domain="[('state','=','approved')]"/>
          <filter string="Posted" name="posted" domain="[('state','=','posted')]"/>
          <separator/>
          <filter string="Today" name="today" date="date"/>
        </search>
      </field>
    </record>

    <!-- Transfer Tree -->
    <record model="ir.ui.view" id="view_pc_transfer_tree">
      <field name="name">Transfer</field>
      <field name="model">accounting.petty.cash.transfer</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="date"/>
          <field name="source_fund_id"/>
          <field name="target_fund_id"/>
          <field name="amount" widget="monetary" sum="Total"/>
          <field name="state" widget="badge" decoration-success="state == 'posted'" decoration-warning="state in ('draft','wait_approval','approved')" decoration-danger="state == 'cancelled'"/>
        </tree>
      </field>
    </record>

    <!-- Transfer Form -->
    <record id="view_pc_transfer_form" model="ir.ui.view">
      <field name="name">accounting.petty.cash.transfer.form</field>
      <field name="model">accounting.petty.cash.transfer</field>
      <field name="arch" type="xml">
      <form string="Transfer" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <field name="state" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or state == 'posted' or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or state == 'posted' or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit or context.get('is_approval_view')" class="oe_highlight"/>
          <button string="Save" name="action_save_revised" type="object" invisible="id and not is_edit or not context.get('is_approval_view')" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this transfer permanently?" invisible="not id or is_edit or state == 'posted' or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or state == 'posted' or user_can_delete"/>
          <button string="Post" name="action_post" type="object" class="btn-primary" invisible="not id or ((state != 'draft' or need_approval or is_edit) and (state != 'approved' or not need_approval))"/>
          <button string="Post" class="opacity-50 pe-none" invisible="not id or not ((state != 'draft' or need_approval or is_edit) and (state != 'approved' or not need_approval))"/>
          <button string="Submit" name="action_submit_for_approval" type="object" class="btn-primary" invisible="state != 'draft' or not need_approval or is_edit or not user_can_submit"/>
          <button string="Approve" name="action_approve" type="object" class="btn-primary" invisible="not id or is_edit or not user_can_approve or not context.get('is_approval_view')"/>
          <button string="Revise" name="action_revise" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_revise or not context.get('is_approval_view')"/>
          <button string="Return" name="action_return" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_return or not context.get('is_approval_view')"/>
          <button string="Reject" name="action_reject" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_reject or not context.get('is_approval_view')"/>
          <button string="Cancel Transfer" name="action_cancel" type="object" class="btn-warning" invisible="not id or state != 'posted' or is_edit"/>
          <button string="Journal Entry" name="action_view_move" type="object" class="btn-secondary" invisible="not move_id"/>
          <field name="state" widget="statusbar" statusbar_visible="draft,wait_approval,approved,posted"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <field name="need_approval" invisible="1"/>
          <field name="approval_status" invisible="1"/>
          <field name="current_approver" invisible="1"/>
          <field name="current_approver_name" invisible="1"/>
          <field name="user_can_approve" invisible="1"/>
          <field name="user_can_revise" invisible="1"/>
          <field name="user_can_return" invisible="1"/>
          <field name="user_can_reject" invisible="1"/>
          <field name="user_can_submit" invisible="1"/>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <field name="name" readonly="1"/>
            <h1>
              <field name="reference" placeholder="e.g. Transfer saldo ke cabang" readonly="not is_edit and id"/>
            </h1>
          </div>
          <group>
            <group>
              <field name="date" readonly="not is_edit and id"/>
              <field name="source_fund_id" readonly="not is_edit and id"/>
              <field name="target_fund_id" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="amount" widget="monetary" readonly="not is_edit and id"/>
              <field name="move_id" invisible="1"/>
            </group>
          </group>
          <notebook>
            <page string="Approval Logs" name="approval_logs" invisible="not approval_log_ids">
              <field name="approval_log_ids" readonly="1">
                <tree default_order="sequence asc, id asc">
                  <field name="sequence" widget="handle"/>
                  <field name="approver" string="Approver Name"/>
                  <field name="position" string="Position/Role"/>
                  <field name="action_date" string="Date"/>
                  <field name="state" decoration-danger="state == 'rejected'" decoration-info="state == 'revised'" decoration-primary="state == 'approved'" decoration-warning="state == 'returned'" widget="badge" string="Status"/>
                  <field name="note" string="Notes/Feedback"/>
                </tree>
                <form string="Approval Log Detail">
                  <group>
                    <field name="approver"/>
                    <field name="position"/>
                    <field name="action_date"/>
                    <field name="state" widget="badge"/>
                  </group>
                  <separator string="Notes"/>
                  <field name="note" readonly="1"/>
                </form>
              </field>
            </page>
          </notebook>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="view_pc_transfer_search">
      <field name="name">Transfer Search</field>
      <field name="model">accounting.petty.cash.transfer</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="date"/>
          <field name="source_fund_id"/>
          <field name="target_fund_id"/>
          <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
          <filter string="Waiting Approval" name="wait_approval" domain="[('state','=','wait_approval')]"/>
          <filter string="To Approve" name="filter_to_approve" domain="[('current_approver','=',uid),('state','=','wait_approval')]"/>
          <filter string="Approved" name="approved" domain="[('state','=','approved')]"/>
          <filter string="Posted" name="posted" domain="[('state','=','posted')]"/>
          <separator/>
          <filter string="Today" name="today" date="date"/>
        </search>
      </field>
    </record>

    <!-- Settlement Tree -->
    <record model="ir.ui.view" id="view_pc_settlement_tree">
      <field name="name">Settlement</field>
      <field name="model">accounting.petty.cash.settlement</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="date"/>
          <field name="fund_id"/>
          <field name="employee_id"/>
          <field name="advance_account_id"/>
          <field name="amount" widget="monetary" sum="Total"/>
          <field name="state" widget="badge" decoration-success="state == 'posted'" decoration-warning="state in ('draft','wait_approval','approved')" decoration-danger="state == 'cancelled'"/>
        </tree>
      </field>
    </record>

    <!-- Settlement Form -->
    <record id="view_pc_settlement_form" model="ir.ui.view">
      <field name="name">accounting.petty.cash.settlement.form</field>
      <field name="model">accounting.petty.cash.settlement</field>
      <field name="arch" type="xml">
      <form string="Settlement" create="false">
        <header>
          <field name="id" invisible="1"/>
          <field name="is_edit" invisible="1"/>
          <field name="user_can_update" invisible="1"/>
          <field name="user_can_delete" invisible="1"/>
          <field name="state" invisible="1"/>
          <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
          <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or state == 'posted' or not user_can_update"/>
          <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or state == 'posted' or user_can_update"/>
          <button string="Save" name="action_save" type="object" invisible="id and not is_edit or context.get('is_approval_view')" class="oe_highlight"/>
          <button string="Save" name="action_save_revised" type="object" invisible="id and not is_edit or not context.get('is_approval_view')" class="oe_highlight"/>
          <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
          <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
          <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this settlement permanently?" invisible="not id or is_edit or state == 'posted' or not user_can_delete"/>
          <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or state == 'posted' or user_can_delete"/>
          <button string="Post" name="action_post" type="object" class="btn-primary" invisible="not id or ((state != 'draft' or need_approval or is_edit) and (state != 'approved' or not need_approval))"/>
          <button string="Post" class="opacity-50 pe-none" invisible="not id or not ((state != 'draft' or need_approval or is_edit) and (state != 'approved' or not need_approval))"/>
          <button string="Submit" name="action_submit_for_approval" type="object" class="btn-primary" invisible="state != 'draft' or not need_approval or is_edit or not user_can_submit"/>
          <button string="Approve" name="action_approve" type="object" class="btn-primary" invisible="not id or is_edit or not user_can_approve or not context.get('is_approval_view')"/>
          <button string="Revise" name="action_revise" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_revise or not context.get('is_approval_view')"/>
          <button string="Return" name="action_return" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_return or not context.get('is_approval_view')"/>
          <button string="Reject" name="action_reject" type="object" class="btn-secondary" invisible="not id or is_edit or not user_can_reject or not context.get('is_approval_view')"/>
          <button string="Cancel Settlement" name="action_cancel" type="object" class="btn-warning" invisible="not id or state != 'posted' or is_edit"/>
          <button string="Journal Entry" name="action_view_move" type="object" class="btn-secondary" invisible="not move_id"/>
          <field name="state" widget="statusbar" statusbar_visible="draft,wait_approval,approved,posted"/>
          <span class="min-w-0 text-truncate mt-1">
            <field name="model_description"/>
          </span>
          <field name="need_approval" invisible="1"/>
          <field name="approval_status" invisible="1"/>
          <field name="current_approver" invisible="1"/>
          <field name="current_approver_name" invisible="1"/>
          <field name="user_can_approve" invisible="1"/>
          <field name="user_can_revise" invisible="1"/>
          <field name="user_can_return" invisible="1"/>
          <field name="user_can_reject" invisible="1"/>
          <field name="user_can_submit" invisible="1"/>
          <style>
            .o_control_panel {
              display: none !important;
            }
          </style>
        </header>
        <sheet>
          <div class="oe_title">
            <field name="name" readonly="1"/>
            <h1>
              <field name="reference" placeholder="e.g. Pengembalian sisa dana" readonly="not is_edit and id"/>
            </h1>
          </div>
          <group>
            <group>
              <field name="date" readonly="not is_edit and id"/>
              <field name="fund_id" readonly="not is_edit and id"/>
              <field name="employee_id" readonly="not is_edit and id"/>
            </group>
            <group>
              <field name="advance_account_id" readonly="not is_edit and id"/>
              <field name="amount" widget="monetary" readonly="not is_edit and id"/>
              <field name="move_id" invisible="1"/>
            </group>
          </group>
          <notebook>
            <page string="Approval Logs" name="approval_logs" invisible="not approval_log_ids">
              <field name="approval_log_ids" readonly="1">
                <tree default_order="sequence asc, id asc">
                  <field name="sequence" widget="handle"/>
                  <field name="approver" string="Approver Name"/>
                  <field name="position" string="Position/Role"/>
                  <field name="action_date" string="Date"/>
                  <field name="state" decoration-danger="state == 'rejected'" decoration-info="state == 'revised'" decoration-primary="state == 'approved'" decoration-warning="state == 'returned'" widget="badge" string="Status"/>
                  <field name="note" string="Notes/Feedback"/>
                </tree>
                <form string="Approval Log Detail">
                  <group>
                    <field name="approver"/>
                    <field name="position"/>
                    <field name="action_date"/>
                    <field name="state" widget="badge"/>
                  </group>
                  <separator string="Notes"/>
                  <field name="note" readonly="1"/>
                </form>
              </field>
            </page>
          </notebook>
        </sheet>
      </form>
      </field>
    </record>

    <record model="ir.ui.view" id="view_pc_settlement_search">
      <field name="name">Settlement Search</field>
      <field name="model">accounting.petty.cash.settlement</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="date"/>
          <field name="fund_id"/>
          <field name="employee_id"/>
          <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
          <filter string="Waiting Approval" name="wait_approval" domain="[('state','=','wait_approval')]"/>
          <filter string="To Approve" name="filter_to_approve" domain="[('current_approver','=',uid),('state','=','wait_approval')]"/>
          <filter string="Approved" name="approved" domain="[('state','=','approved')]"/>
          <filter string="Posted" name="posted" domain="[('state','=','posted')]"/>
          <separator/>
          <filter string="Today" name="today" date="date"/>
        </search>
      </field>
    </record>

    <!-- ================================================================= -->
    <!-- PETTY CASH ACTIONS                                                 -->
    <!-- ================================================================= -->

    <record model="ir.actions.act_window" id="action_petty_cash">
      <field name="name">Petty Cash Funds</field>
      <field name="res_model">accounting.petty.cash</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <record model="ir.actions.act_window" id="action_pc_category">
      <field name="name">Expense Categories</field>
      <field name="res_model">accounting.petty.cash.category</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <record model="ir.actions.act_window" id="action_pc_expense">
      <field name="name">Cash Expenses</field>
      <field name="res_model">accounting.petty.cash.expense</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <record model="ir.actions.act_window" id="action_pc_topup">
      <field name="name">Top Ups</field>
      <field name="res_model">accounting.petty.cash.topup</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <record model="ir.actions.act_window" id="action_pc_transfer">
      <field name="name">Transfers</field>
      <field name="res_model">accounting.petty.cash.transfer</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <record model="ir.actions.act_window" id="action_pc_settlement">
      <field name="name">Settlements</field>
      <field name="res_model">accounting.petty.cash.settlement</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
    </record>

    <!-- ================================================================= -->
    <!-- PETTY CASH MENU ITEMS                                             -->
    <!-- ================================================================= -->

    <menuitem name="Petty Cash" id="menu_pc_parent" parent="menu_accounting_transactions_parent" sequence="2"/>
    <menuitem name="Cash Expenses" id="menu_pc_expenses_act" parent="menu_pc_parent" action="action_pc_expense" sequence="1"/>
    <menuitem name="Top Ups" id="menu_pc_topups_act" parent="menu_pc_parent" action="action_pc_topup" sequence="2"/>
    <menuitem name="Transfers" id="menu_pc_transfers_act" parent="menu_pc_parent" action="action_pc_transfer" sequence="3"/>
    <menuitem name="Settlements" id="menu_pc_settlements_act" parent="menu_pc_parent" action="action_pc_settlement" sequence="4"/>

    <menuitem name="Petty Cash Funds" id="menu_petty_cash_funds_act" parent="menu_accounting_config_parent" action="action_petty_cash" sequence="6"/>
    <menuitem name="Expense Categories" id="menu_pc_categories_act" parent="menu_accounting_config_parent" action="action_pc_category" sequence="7"/>

    <!-- ================================================================= -->
    <!-- APPROVAL WIZARD                                                    -->
    <!-- ================================================================= -->

    <record id="view_accounting_approve_wizard_form" model="ir.ui.view">
      <field name="name">accounting.approve.wizard.form</field>
      <field name="model">accounting.approve.wizard</field>
      <field name="arch" type="xml">
        <form string="Approval Confirmation">
          <field name="document_type" invisible="1"/>
          <field name="record_id" invisible="1"/>
          <div class="alert alert-info" role="alert" style="margin-bottom:0px;">
            <field name="message"/>
          </div>
          <footer>
            <button name="action_confirm" string="Yes, Approved" type="object"
                    class="btn-primary"/>
            <button string="Cancel" class="btn-secondary" special="cancel"/>
          </footer>
        </form>
      </field>
    </record>

    <record id="view_accounting_reject_wizard_form" model="ir.ui.view">
      <field name="name">accounting.reject.wizard.form</field>
      <field name="model">accounting.reject.wizard</field>
      <field name="arch" type="xml">
        <form string="Reject Confirmation">
          <field name="document_type" invisible="1"/>
          <field name="record_id" invisible="1"/>
          <label for="reason" string="Reason"/>
          <field name="reason"
                 placeholder="Please provide a reason for rejection..."/>
          <footer>
            <button name="action_reject_confirm" string="Confirm Reject" type="object"
                    class="btn-primary"/>
            <button string="Cancel" class="btn-secondary" special="cancel"/>
          </footer>
        </form>
      </field>
    </record>

    <record id="view_accounting_return_wizard_form" model="ir.ui.view">
      <field name="name">accounting.return.wizard.form</field>
      <field name="model">accounting.return.wizard</field>
      <field name="arch" type="xml">
        <form string="Return Reason">
          <field name="document_type" invisible="1"/>
          <field name="record_id" invisible="1"/>
          <label for="reason" string="Reason"/>
          <field name="reason"
                 placeholder="Please provide a reason for returning this document..."/>
          <footer>
            <button name="action_return_confirm" string="Confirm Return" type="object"
                    class="btn-primary"/>
            <button string="Cancel" class="btn-secondary" special="cancel"/>
          </footer>
        </form>
      </field>
    </record>

    <record id="view_accounting_revise_wizard_form" model="ir.ui.view">
      <field name="name">accounting.revise.wizard.form</field>
      <field name="model">accounting.revise.wizard</field>
      <field name="arch" type="xml">
        <form string="Revise Message">
          <field name="document_type" invisible="1"/>
          <field name="record_id" invisible="1"/>
          <label for="message" string="Message"/>
          <field name="message"
                 placeholder="Please provide a message for the revision..."/>
          <footer>
            <button name="action_revise_confirm" string="Confirm Revise" type="object"
                    class="btn-primary"/>
            <button string="Cancel" class="btn-secondary" special="cancel"/>
          </footer>
        </form>
      </field>
    </record>

    <!-- ===================================================================== -->
    <!-- APPROVAL MATRIX                                                      -->
    <!-- ===================================================================== -->

    <record model="ir.ui.view" id="view_accounting_approval_matrix_tree">
      <field name="name">Accounting Approval Matrix Tree</field>
      <field name="model">accounting.approval.matrix</field>
      <field name="arch" type="xml">
        <tree string="Approval Matrix">
          <field name="document_type" widget="badge"/>
          <field name="sequence" widget="handle"/>
          <field name="name"/>
          <field name="position"/>
          <field name="min_amount" widget="monetary"/>
          <field name="receive_return" invisible="1"/>
          <field name="approve"/>
          <field name="revise"/>
          <field name="returned"/>
          <field name="reject"/>
          <field name="notify"/>
        </tree>
      </field>
    </record>

    <record model="ir.ui.view" id="view_accounting_approval_matrix_form">
      <field name="name">Accounting Approval Matrix Form</field>
      <field name="model">accounting.approval.matrix</field>
      <field name="arch" type="xml">
        <form string="Approval Matrix" create="false">
          <header>
            <field name="id" invisible="1"/>
            <field name="is_edit" invisible="1"/>
            <field name="user_can_update" invisible="1"/>
            <field name="user_can_delete" invisible="1"/>
            <button string="Back" name="action_back" type="object" invisible="is_edit or not id"/>
            <button string="Edit" name="action_edit" type="object" invisible="not id or is_edit or not user_can_update"/>
            <button string="Edit" class="opacity-50 pe-none" invisible="not id or is_edit or user_can_update"/>
            <button string="Save" name="action_save" type="object" invisible="id and not is_edit" class="oe_highlight"/>
            <button string="Cancel" invisible="id" special="cancel" class="discard-new"/>
            <button string="Cancel" invisible="not is_edit" special="cancel" class="discard-edit"/>
            <button name="action_delete" string="Delete" type="object" class="oe_highlight" confirm="Are you sure you want to delete this data permanently?" invisible="not id or is_edit or not user_can_delete"/>
            <button string="Delete" class="oe_highlight opacity-50 pe-none" invisible="not id or is_edit or user_can_delete"/>
            <style>
              .o_control_panel {
                display: none !important;
              }
            </style>
          </header>
          <sheet>
            <group>
              <group>
                <field name="document_type" required="1" readonly="not is_edit and id"/>
                <field name="name" required="1" readonly="not is_edit and id"/>
                <field name="position" readonly="1"/>
                <field name="sequence" readonly="not is_edit and id"/>
              </group>
              <group>
                <field name="min_amount" readonly="not is_edit and id"/>
                <field name="approved_as" readonly="not is_edit and id"/>
                <field name="receive_return" readonly="not is_edit and id"/>
              </group>
            </group>
            <group string="Permissions">
              <group>
                <field name="approve" readonly="not is_edit and id"/>
                <field name="revise" readonly="not is_edit and id"/>
                <field name="printed" readonly="not is_edit and id"/>
              </group>
              <group>
                <field name="returned" readonly="not is_edit and id"/>
                <field name="reject" readonly="not is_edit and id"/>
                <field name="notify" readonly="not is_edit and id"/>
              </group>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <record model="ir.actions.act_window" id="action_accounting_approval_matrix">
      <field name="name">Accounting Approval Matrix</field>
      <field name="res_model">accounting.approval.matrix</field>
      <field name="view_mode">tree,form</field>
      <field name="target">main</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">Create your first Accounting Approval Matrix</p>
        <p>Define the approver roster and their allowed actions for each petty cash document.</p>
      </field>
    </record>

    <menuitem name="Accounting Matrix" id="menu_accounting_approval_matrix_act" parent="sales.approval_matrix_menu" action="action_accounting_approval_matrix" sequence="3"/>

    <!-- ===================================================================== -->
    <!-- WAITING FOR APPROVAL (per document type)                             -->
    <!-- ===================================================================== -->

    <record id="action_waiting_approval_expense" model="ir.actions.act_window">
      <field name="name">Waiting My Approval</field>
      <field name="res_model">accounting.petty.cash.expense</field>
      <field name="view_mode">tree,form</field>
      <field name="domain">[('current_approver','=',uid),('state','=','wait_approval')]</field>
      <field name="context">{'search_default_filter_to_approve': 1, 'create': False, 'is_approval_view': True}</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">No documents waiting for your approval.</p>
      </field>
    </record>

    <record id="action_waiting_approval_topup" model="ir.actions.act_window">
      <field name="name">Waiting for My Approval</field>
      <field name="res_model">accounting.petty.cash.topup</field>
      <field name="view_mode">tree,form</field>
      <field name="domain">[('current_approver','=',uid),('state','=','wait_approval')]</field>
      <field name="context">{'search_default_filter_to_approve': 1, 'create': False, 'is_approval_view': True}</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">No documents waiting for your approval.</p>
      </field>
    </record>

    <record id="action_waiting_approval_transfer" model="ir.actions.act_window">
      <field name="name">Waiting for My Approval</field>
      <field name="res_model">accounting.petty.cash.transfer</field>
      <field name="view_mode">tree,form</field>
      <field name="domain">[('current_approver','=',uid),('state','=','wait_approval')]</field>
      <field name="context">{'search_default_filter_to_approve': 1, 'create': False, 'is_approval_view': True}</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">No documents waiting for your approval.</p>
      </field>
    </record>

    <record id="action_waiting_approval_settlement" model="ir.actions.act_window">
      <field name="name">Waiting for My Approval</field>
      <field name="res_model">accounting.petty.cash.settlement</field>
      <field name="view_mode">tree,form</field>
      <field name="domain">[('current_approver','=',uid),('state','=','wait_approval')]</field>
      <field name="context">{'search_default_filter_to_approve': 1, 'create': False, 'is_approval_view': True}</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">No documents waiting for your approval.</p>
      </field>
    </record>

    <!-- Approval submenu under Approvals -->
    <menuitem name="Accounting" id="menu_accounting_approval_parent" parent="sales.menu_approvals_root" sequence="3"/>
    <menuitem name="Expenses to Approve" id="menu_wait_approval_expense" parent="menu_accounting_approval_parent" action="action_waiting_approval_expense" sequence="1"/>
    <menuitem name="Top Ups to Approve" id="menu_wait_approval_topup" parent="menu_accounting_approval_parent" action="action_waiting_approval_topup" sequence="2"/>
    <menuitem name="Transfers to Approve" id="menu_wait_approval_transfer" parent="menu_accounting_approval_parent" action="action_waiting_approval_transfer" sequence="3"/>
    <menuitem name="Settlements to Approve" id="menu_wait_approval_settlement" parent="menu_accounting_approval_parent" action="action_waiting_approval_settlement" sequence="4"/>

    <!-- RBAC: general.menu entries for the accounting "to Approve" menus.
         The ir_ui_menu_id MUST point to the exact ir.ui.menu record above
         (record ref, not name match), so users without a general.auth entry
         for these menus are restricted from seeing them. Placed here (after
         the <menuitem> records) so the refs resolve regardless of the
         manifest data load order (data/menu.xml is loaded before views.xml). -->
    <record id="accounting.menu_accounting_approvals" model="general.menu">
      <field name="menu_name">Accounting</field>
      <field name="ir_ui_menu_id" ref="accounting.menu_accounting_approval_parent"/>
    </record>
    <record id="menu_wait_approval_expense_menu" model="general.menu">
      <field name="menu_id">pc_approval_expense</field>
      <field name="menu_name">Expenses to Approve</field>
      <field name="parent_menu">accounting_approvals</field>
      <field name="ir_ui_menu_id" ref="accounting.menu_wait_approval_expense"/>
    </record>
    <record id="menu_wait_approval_topup_menu" model="general.menu">
      <field name="menu_id">pc_approval_topup</field>
      <field name="menu_name">Top Ups to Approve</field>
      <field name="parent_menu">accounting_approvals</field>
      <field name="ir_ui_menu_id" ref="accounting.menu_wait_approval_topup"/>
    </record>
    <record id="menu_wait_approval_transfer_menu" model="general.menu">
      <field name="menu_id">pc_approval_transfer</field>
      <field name="menu_name">Transfers to Approve</field>
      <field name="parent_menu">accounting_approvals</field>
      <field name="ir_ui_menu_id" ref="accounting.menu_wait_approval_transfer"/>
    </record>
    <record id="menu_wait_approval_settlement_menu" model="general.menu">
      <field name="menu_id">pc_approval_settlement</field>
      <field name="menu_name">Settlements to Approve</field>
      <field name="parent_menu">accounting_approvals</field>
      <field name="ir_ui_menu_id" ref="accounting.menu_wait_approval_settlement"/>
    </record>

    <!-- RBAC: link the Accounting Matrix menu (record created in data/menu.xml)
         to its exact ir.ui.menu record so it can be granted/restricted too. -->
    <record id="accounting.menu_accounting_approval_matrix" model="general.menu">
      <field name="ir_ui_menu_id" ref="accounting.menu_accounting_approval_matrix_act"/>
    </record>

    <!-- SERVICE CATEGORY: inject expense_account_id via inheritance -->
    <record id="purchases_service_category_tree_inherit" model="ir.ui.view">
      <field name="name">purchases.service_category.tree.inherit</field>
      <field name="model">purchases.service_category</field>
      <field name="inherit_id" ref="purchases.purchases_service_category_tree"/>
      <field name="arch" type="xml">
        <field name="category_name" position="after">
          <field name="expense_account_id"/>
        </field>
      </field>
    </record>

    <record id="purchases_service_category_form_inherit" model="ir.ui.view">
      <field name="name">purchases.service_category.form.inherit</field>
      <field name="model">purchases.service_category</field>
      <field name="inherit_id" ref="purchases.purchases_service_category_form"/>
      <field name="arch" type="xml">
        <form>
          <sheet>
            <group>
              <group>
                <field name="expense_account_id"/>
              </group>
            </group>
          </sheet>
        </form>
      </field>
    </record>

  </data>
</odoo>
